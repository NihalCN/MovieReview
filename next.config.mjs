/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['images-cdn.ubuy.co.in','image.tmdb.org'],
    },
};

export default nextConfig;
